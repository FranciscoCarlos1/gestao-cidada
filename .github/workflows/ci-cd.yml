name: CI/CD (Deprecated)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'Deprecated pipeline kept for reference. Use ci.yml.'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Deprecated: use .github/workflows/ci.yml'
