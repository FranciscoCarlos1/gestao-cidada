name: Release on Tag (Deprecated)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'Deprecated release workflow. Use android-release.yml.'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Deprecated: use .github/workflows/android-release.yml'
